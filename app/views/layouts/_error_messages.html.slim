- if object.errors.any?
  .message.is-danger
    .message-header The form contains #{pluralize(object.errors.count, 'error')}
    .message-body
      ul
        - object.errors.each do |message|
          li = message
